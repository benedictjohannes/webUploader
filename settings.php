<?php 
// time to expiration of session
$expireTimeout = 1800 ;

// $webuploader_limit
// $upload_size_limit


// Enable access management
$wuPermissionEnable = 0;

// size limit for the whole WebUploader folder:
$wuTotalSizeLimit = 1024;

// overwrite oldest file upon hitting WebUploader whole folder size limit:
$wuOverwriteOldest = 1;


// maximum upload file size limit
$wuMaxUploadSize = 0;

// A folder for each user
$wuUserHome = 1;

// Multiple Shared Folder
$wuMultipleShares = 1;

// user database settings 
$dbhost = 'localhost';
$dbname = 'webuploader';
$dbuser = 'wwwLocal';
$dbpass = 'wwwLocalAdmin';



